---
import { siteConfig } from '@config/site';
import { withBase } from '@utils/url';

const home = withBase('/');

const currentPath = Astro.url.pathname.replace(/\/$/, '') || '/';
const isActive = (href: string) => {
  const target = withBase(href).replace(/\/$/, '');
  return currentPath === target || currentPath.startsWith(`${target}/`);
};
---

<header class="site-header">
  <div class="header-inner">
    <a href={home} class="site-name">{siteConfig.name}</a>
    <nav class="site-nav work-nav" aria-label="Work navigation">
      <ul role="list">
        {siteConfig.nav.work.map(link => (
          <li><a href={withBase(link.href)} aria-current={isActive(link.href) ? 'page' : undefined}>{link.label}</a></li>
        ))}
      </ul>
    </nav>
    <nav class="site-nav pages-nav" aria-label="Pages navigation">
      <ul role="list">
        {siteConfig.nav.pages.map(link => (
          <li><a href={withBase(link.href)} aria-current={isActive(link.href) ? 'page' : undefined}>{link.label}</a></li>
        ))}
      </ul>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 50;
    background: var(--color-bg);
  }

  .header-inner {
    padding: var(--space-md) var(--space-lg);
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    column-gap: var(--space-md);
  }

  .site-name {
    font-size: 1rem;
    color: var(--color-text);
    justify-self: start;
  }

  .work-nav {
    justify-self: center;
  }

  .pages-nav {
    justify-self: end;
  }

  .site-nav ul {
    display: flex;
    gap: var(--space-lg);
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .site-nav a[aria-current="page"] {
    opacity: 0.6;
  }

  @media (max-width: 640px) {
    .header-inner {
      padding-inline: var(--space-md);
      column-gap: var(--space-sm);
    }

    .site-nav ul {
      gap: var(--space-md);
    }

    .site-name {
      font-size: 0.875rem;
    }
  }
</style>
